from setuptools import setup, find_packages

setup(
    name='myutilpy',
    version='0.1',
    packages=find_packages(),
    description='A python helper utilities package for the nlp-projects repo.',
)